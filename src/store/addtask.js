const addtask = (state = 0, action) => {
    switch(action.type) {
        case 'ADD_TASK':
        // setState({
        //     tasks: [
        //         state.tasks, newTask
        //     ]
        // })
        return state
        default:
            return state
    }
}

export default addtask;